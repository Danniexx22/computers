{% extends 'base.html.twig' %}

{% block title %}Coordinacion{% endblock %}

{% block body %}
    <h1>Coordinacion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coordinacion.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ coordinacion.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('coordinacion_index') }}">back to list</a>

    <a href="{{ path('coordinacion_edit', {'id': coordinacion.id}) }}">edit</a>

    {{ include('coordinacion/_delete_form.html.twig') }}
{% endblock %}