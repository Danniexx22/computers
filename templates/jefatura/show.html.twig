{% extends 'base.html.twig' %}

{% block title %}Jefatura{% endblock %}

{% block body %}
    <h1>Jefatura</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jefatura.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ jefatura.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('jefatura_index') }}">back to list</a>

    <a href="{{ path('jefatura_edit', {'id': jefatura.id}) }}">edit</a>

    {{ include('jefatura/_delete_form.html.twig') }}
{% endblock %}