{% extends 'base.html.twig' %}

{% block title %}Modelo{% endblock %}

{% block contenido %}
    <h1>Modelo</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ modelo.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ modelo.name }}</td>
            </tr>
            <tr>
                <th>Ram</th>
                <td>{{ modelo.ram }}</td>
            </tr>
            <tr>
                <th>Harddisk</th>
                <td>{{ modelo.harddisk }}</td>
            </tr>
            <tr>
                <th>Procesor</th>
                <td>{{ modelo.procesor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('modelo_index') }}">back to list</a>

    <a href="{{ path('modelo_edit', {'id': modelo.id}) }}">edit</a>

    {{ include('modelo/_delete_form.html.twig') }}
{% endblock %}