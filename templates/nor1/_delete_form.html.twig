<form method="post" action="{{ path('nor1_delete', {'id': nor1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nor1.id) }}">
    <button class="btn">Delete</button>
</form>