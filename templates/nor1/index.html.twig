{% extends 'base.html.twig' %}

{% block title %}Nor1 index{% endblock %}

{% block contenido %}
    <h1>Nor1 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for nor1 in nor1s %}
            <tr>
                <td>{{ nor1.id }}</td>
                <td>{{ nor1.descripcion }}</td>
                <td>
                    <a href="{{ path('nor1_show', {'id': nor1.id}) }}">show</a>
                    <a href="{{ path('nor1_edit', {'id': nor1.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('nor1_new') }}">Create new</a>
{% endblock %}