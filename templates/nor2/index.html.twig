{% extends 'base.html.twig' %}

{% block title %}Nor2 index{% endblock %}

{% block body %}
    <h1>Nor2 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for nor2 in nor2s %}
            <tr>
                <td>{{ nor2.id }}</td>
                <td>{{ nor2.descripcion }}</td>
                <td>
                    <a href="{{ path('nor2_show', {'id': nor2.id}) }}">show</a>
                    <a href="{{ path('nor2_edit', {'id': nor2.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('nor2_new') }}">Create new</a>
{% endblock %}