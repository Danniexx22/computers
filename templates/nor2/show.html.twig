{% extends 'base.html.twig' %}

{% block title %}Nor2{% endblock %}

{% block body %}
    <h1>Nor2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nor2.id }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ nor2.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('nor2_index') }}">back to list</a>

    <a href="{{ path('nor2_edit', {'id': nor2.id}) }}">edit</a>

    {{ include('nor2/_delete_form.html.twig') }}
{% endblock %}