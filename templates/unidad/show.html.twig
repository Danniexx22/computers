{% extends 'base.html.twig' %}

{% block title %}Unidad{% endblock %}

{% block contenido %}
    <h1>Unidad</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ unidad.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ unidad.name }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ unidad.address }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('unidad_index') }}">back to list</a>

    <a href="{{ path('unidad_edit', {'id': unidad.id}) }}">edit</a>

    {{ include('unidad/_delete_form.html.twig') }}
{% endblock %}